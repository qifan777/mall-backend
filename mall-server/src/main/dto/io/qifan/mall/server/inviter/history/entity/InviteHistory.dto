
input InviteHistoryInput {
    #allScalars(InviteHistory)
    id?
}

specification InviteHistorySpec {
    #allScalars
    inviter{
        id
    }
    like/i(id)
    ge(editedTime)
    le(editedTime)
    ge(createdTime)
    le(createdTime)
}