
input AgentInput {
    #allScalars(Agent)
    id?
    userId
    agentLevel{
      levelName
    }
}

specification AgentSpec {
    #allScalars
    like/i(tenantId)
    like/i(parentId)
    like/i(agentNo)
    like/i(id)
    ge(createdTime)
    le(createdTime)
    ge(editedTime)
    le(editedTime)
    id(user)
    agentLevel{
      levelName
    }
}