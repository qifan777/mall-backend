
input AgentRebateRecordInput {
    #allScalars(AgentRebateRecord)
    agentId
    walletRecordId
    productOrderId
    fromAgentId
    id?
}

specification AgentRebateRecordSpec {
    #allScalars
    ge(createdTime)
    le(createdTime)
    ge(editedTime)
    le(editedTime)
    agent{
      id
      userId
      agentNo
    }
    fromAgent{
      id
      userId
      agentNo
    }
    walletRecordId
    productOrderId
}